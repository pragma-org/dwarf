{% extends "_base.j2" %}
{% block main %}
<span class="eyebrow">Learn</span>
<h1 class="learn-hero">A forensic testing framework for Cardano nodes.</h1>
<p>Dwarf feeds adversarial inputs to Amaru and the Haskell <code>cardano-node</code>, then records every probe as a tamper-evident evidence bundle. The pages below walk through how the substrate is structured, where coverage stands, and how to read what the watcher sees.</p>

<hr class="hairline">

<div class="learn-tile-grid">

  <a class="learn-tile" href="/learn/overview">
    <span class="learn-tile__eyebrow">The whole workbench, one page</span>
    <h2 class="learn-tile__title">Overview</h2>
    <p class="learn-tile__sub">The full self-contained DWARF reference — DSL, the {{ primitive_count }}-primitive catalogue, Antithesis pipeline, native coverage, evidence bundles, and the server-refreshed attack-cost estimate. The complete walkthrough.</p>
  </a>

  <a class="learn-tile" href="/learn/measurements">
    <span class="learn-tile__eyebrow">{{ measurement_count }} measurement definitions · {{ measurement_profile_count }} profiles</span>
    <h2 class="learn-tile__title">Measurements</h2>
    <p class="learn-tile__sub">Learn how Stock, External, Patched, and Reserved sources become metrics, how profiles select taps, and what retained run evidence can and cannot prove.</p>
  </a>

  <a class="learn-tile" href="/learn/measurement-coverage">
    <span class="learn-tile__eyebrow">Applicability × workload × retained proof</span>
    <h2 class="learn-tile__title">Measurement coverage</h2>
    <p class="learn-tile__sub">Join threats, risks, scenario families, and node surfaces to compatible taps and exact five-card evidence. See what is applicable, configured, exercised, verified, unavailable, or reserved.</p>
  </a>

  <a class="learn-tile" href="/learn/attack-cost">
    <span class="learn-tile__eyebrow">On-chain stake + ADA price</span>
    <h2 class="learn-tile__title">Attack cost</h2>
    <p class="learn-tile__sub">The effective ~40%-of-active-stake threshold in ADA and USD, using briefly cached server-side data with an explicitly labelled snapshot fallback.</p>
  </a>

  <a class="learn-tile" href="/learn/threat-coverage">
    <span class="learn-tile__eyebrow">Risk Register v2 × Threat Model v2 × scenarios</span>
    <h2 class="learn-tile__title">Threat / risk coverage map</h2>
    <p class="learn-tile__sub">Every DWARF scenario mapped to the Amaru Risk Register (RR-001…035) and Threat Model (TM-001…036) via a vetted per-concern mapping — with scripted/fuzz/smoke maturity pills, a surface bar chart, live search + filters, and explicit GAP rows for threats no scenario yet exercises.</p>
  </a>

  <a class="learn-tile" href="/learn/consensus">
    <span class="learn-tile__eyebrow">cardano-node vs Amaru · differential</span>
    <h2 class="learn-tile__title">Consensus differential</h2>
    <p class="learn-tile__sub">Do the Haskell cardano-node and Amaru ever disagree on which chain is real? Induced forks on the real cardano_amaru topology — honest, &lt;k recovery, &gt;k stranding, epoch boundary — with the k-boundary sweep. Same chain, every time.</p>
  </a>

  <a class="learn-tile" href="/learn/getting-started">
    <span class="learn-tile__eyebrow">5-step Hello-Dwarf walkthrough</span>
    <h2 class="learn-tile__title">Getting started</h2>
    <p class="learn-tile__sub">Install → run your first scenario → find it in the dashboard → preserve a bundle → write your first assertion. Copy-paste-and-go.</p>
  </a>

  <a class="learn-tile" href="/learn/examples">
    <span class="learn-tile__eyebrow">{{ example_count }} annotated scenarios · {{ asset_example_count }} reusable-asset examples</span>
    <h2 class="learn-tile__title">Examples</h2>
    <p class="learn-tile__sub">Real scenario YAML plus one concrete primitive, template, testcase, bucket, corpus, grammar, risk-package, and plugin example with its schema, source, lifecycle, mutability, and relationships.</p>
  </a>

  <a class="learn-tile" href="/learn/primitives">
    <span class="learn-tile__eyebrow">{{ primitive_count }} registered operations · six families</span>
    <h2 class="learn-tile__title">Primitive reference</h2>
    <p class="learn-tile__sub">Registry, parameter schemas, target/runtime compatibility, executor provenance, scenario references, and the code-change lifecycle for adding a primitive.</p>
  </a>

  <a class="learn-tile" href="/learn/profile-templates">
    <span class="learn-tile__eyebrow">{{ profile_template_count }} shipped topology scaffolds</span>
    <h2 class="learn-tile__title">Profile templates</h2>
    <p class="learn-tile__sub">Node mixes, substitutions, topology and network assumptions, immutable source, and the validated path from a shipped scaffold to a new deployment profile.</p>
  </a>

  <a class="learn-tile" href="/learn/versions">
    <span class="learn-tile__eyebrow">latest-confirmed · exact · latest-stable</span>
    <h2 class="learn-tile__title">Version-qualified devnets</h2>
    <p class="learn-tile__sub">How profiles resolve immutable Cardano-node and Amaru releases, distinguish stable from confirmed, gate unknown or incompatible selections, and retain exact deployment evidence.</p>
  </a>

  <a class="learn-tile" href="/learn/corpora">
    <span class="learn-tile__eyebrow">{{ corpus_count }} corpus units · {{ corpus_input_count }} inputs</span>
    <h2 class="learn-tile__title">Fuzz corpora</h2>
    <p class="learn-tile__sub">Seed, generated, queue, crash, minimized, regression, and promoted roles; exact-byte inventory, provenance, hashes, bounded previews, export, and the safe writable overlay.</p>
  </a>

  <a class="learn-tile" href="/learn/grammars">
    <span class="learn-tile__eyebrow">{{ grammar_count }} structure/token sets · {{ grammar_token_count }} parsed tokens</span>
    <h2 class="learn-tile__title">Generation grammars</h2>
    <p class="learn-tile__sub">Machine-readable input-shape metadata and finite mutation tokens, with exact provenance, engine boundaries, validation, clone, and safe runtime editing.</p>
  </a>

  <a class="learn-tile" href="/learn/risk-packages">
    <span class="learn-tile__eyebrow">{{ risk_package_count }} work packages · {{ risk_candidate_count }} candidate-risk IDs</span>
    <h2 class="learn-tile__title">Risk work packages</h2>
    <p class="learn-tile__sub">Milestone-1 planning groups, evidence references, blockers, compatibility, and the boundary between planned work and forensic run bundles.</p>
  </a>

  <a class="learn-tile" href="/learn/walkthroughs">
    <span class="learn-tile__eyebrow">{{ walkthrough_count }} narrated paths</span>
    <h2 class="learn-tile__title">Walkthroughs</h2>
    <p class="learn-tile__sub">Operator paths from <em>Run your first scenario</em> to <em>Compare two implementations</em>. Each step references a real file or page.</p>
  </a>

  <a class="learn-tile" href="/learn/architecture">
    <span class="learn-tile__eyebrow">Annotated SVG diagram</span>
    <h2 class="learn-tile__title">Architecture</h2>
    <p class="learn-tile__sub">The dwarf substrate end to end — telemetry, scenario harness, evidence bundling, comparison, lifecycle.</p>
  </a>

  <a class="learn-tile" href="/learn/concepts">
    <span class="learn-tile__eyebrow">{{ concept_count }} terms · anchored to project paths</span>
    <h2 class="learn-tile__title">Glossary (concepts)</h2>
    <p class="learn-tile__sub">Vocabulary used across the substrate — bucket, lifecycle, scenario, bundle, fault. Each entry resolves to a real file in the repository.</p>
  </a>

  <a class="learn-tile" href="/learn/glossary">
    <span class="learn-tile__eyebrow">{{ glossary_count }} terms · framework → consensus → eras</span>
    <h2 class="learn-tile__title">Glossary (audit)</h2>
    <p class="learn-tile__sub">Audit-grade terminology spanning the DWARF framework (substrate, primitive, assertion, manifest hash chain, replay determinism, AFL++, SARIF, plugin) and Cardano itself — Praos, VRF, KES, k, common prefix, the mini-protocols, all seven eras, and peer governance.</p>
  </a>

  <a class="learn-tile" href="/learn/api">
    <span class="learn-tile__eyebrow">JSON · SSE · binary endpoints</span>
    <h2 class="learn-tile__title">REST API</h2>
    <p class="learn-tile__sub">Reference for the dashboard's machine-readable surface — /healthz, /metrics, /api/health, /api/runs, SSE log tail, bundle export/import. Plus the HTML-only catalog.</p>
  </a>

  <a class="learn-tile" href="/learn/faq">
    <span class="learn-tile__eyebrow">15 common questions</span>
    <h2 class="learn-tile__title">FAQ</h2>
    <p class="learn-tile__sub">First scenario, bundle locations, compose modes, plugins, theme picker, notifications wiring, scraping policy, audit-package export.</p>
  </a>

  <a class="learn-tile" href="/learn/troubleshooting">
    <span class="learn-tile__eyebrow">12 deploy gotchas + fixes</span>
    <h2 class="learn-tile__title">Troubleshooting</h2>
    <p class="learn-tile__sub">rsync silent no-op · DASHBOARD_ROOT path resolution · pgrep parent-vs-listener · docker-cp vs host-mount · /tmp socket 108-byte limit · Caddy SSE 502s.</p>
  </a>

  <a class="learn-tile" href="/learn/operator-runbook">
    <span class="learn-tile__eyebrow">Deploy + day-2 ops</span>
    <h2 class="learn-tile__title">Operator runbook</h2>
    <p class="learn-tile__sub">Docker / bare-metal deploy, common operations, troubleshooting recipes, backup &amp; restore, /healthz + Prometheus monitoring.</p>
  </a>

  <a class="learn-tile" href="/learn/operator-runbook#moog-security-testing">
    <span class="learn-tile__eyebrow">Preprod requester workflow</span>
    <h2 class="learn-tile__title">Moog security testing</h2>
    <p class="learn-tile__sub">Connected wallet status, Preprod faucet handling, local asset scaffold/validation, preflight, GitHub PAT and <code>moog.vkey</code> requirements, requester registration, and CODEOWNERS.</p>
  </a>

  <a class="learn-tile" href="/learn/operator-runbook#antithesis-bundles">
    <span class="learn-tile__eyebrow">One definition, two backends</span>
    <h2 class="learn-tile__title">Antithesis bundles</h2>
    <p class="learn-tile__sub">Render a profile to a hermetic Antithesis bundle with <code>antithesis build</code> — single closed-Amaru, or mixed Haskell+Amaru with a cross-implementation differential. Compose + workload + SDK assertions; the same asset-dir Moog validates and submits; stops at ready-to-submit.</p>
  </a>

  <a class="learn-tile" href="/learn/developer-onboarding">
    <span class="learn-tile__eyebrow">TDD + lane discipline</span>
    <h2 class="learn-tile__title">Developer onboarding</h2>
    <p class="learn-tile__sub">Add a primitive · scenario · assertion · profile. Narrow-commit pattern, local validate flow, multi-lane coordination.</p>
  </a>

  <a class="learn-tile" href="/learn/plugin-authoring">
    <span class="learn-tile__eyebrow">{{ plugin_count }} discovered plugins · extend without forking</span>
    <h2 class="learn-tile__title">Plugin authoring</h2>
    <p class="learn-tile__sub">Plugin discovery model · register() contract · sandbox / security model · hello-world example · publishing patterns.</p>
  </a>

  <a class="learn-tile" href="/learn/coverage">
    <span class="learn-tile__eyebrow">Census + coverage matrices</span>
    <h2 class="learn-tile__title">Coverage</h2>
    <p class="learn-tile__sub">A scenario census that accounts for every scenario once by category (reconciling to the full catalog), plus per-surface matrices — implementations × mini-protocols, CBOR decode surfaces, fault families, and fuzzer backends — showing what is and isn&rsquo;t under probe.</p>
  </a>

  <a class="learn-tile" href="/learn/status">
    <span class="learn-tile__eyebrow">Project status</span>
    <h2 class="learn-tile__title">Status</h2>
    <p class="learn-tile__sub">Current phase, candidate findings under investigation, recent feature commits on <code>main</code>.</p>
  </a>

  <a class="learn-tile" href="/learn/cli">
    <span class="learn-tile__eyebrow">Operator CLI reference</span>
    <h2 class="learn-tile__title">CLI</h2>
    <p class="learn-tile__sub">Every <code>cardano-profile</code> command group with worked examples — scenario, compare, bundle, fuzz, primitive, dashboard, testcase, moog, and antithesis.</p>
  </a>

</div>

<hr class="hairline">

<section class="learn-substrate">
  <span class="eyebrow">Substrate at a glance</span>
  <p>Counts below and on the catalog tiles are loaded from the same source catalogs as Operate; this page does not maintain a second inventory.</p>
  <ul class="data-proof-list">
    <li class="data-proof-row">
      <span class="label">Implementations</span>
      <span class="value"{% if not implementations %} data-empty="true"{% endif %}>{{ implementations|join(', ') if implementations else 'needs source' }}</span>
    </li>
    <li class="data-proof-row">
      <span class="label">Scenarios available</span>
      <span class="value">{{ scenario_count }}</span>
    </li>
    <li class="data-proof-row">
      <span class="label">Fuzz targets</span>
      <span class="value">{{ fuzz_target_count }}</span>
    </li>
    {% if runs_total and pass_rate_pct is not none %}
    <li class="data-proof-row">
      <span class="label">Recent runs (rolling {{ runs_total }})</span>
      <span class="value">{{ pass_rate_pct }}% pass-rate</span>
    </li>
    {% endif %}
  </ul>
</section>
{% endblock %}
