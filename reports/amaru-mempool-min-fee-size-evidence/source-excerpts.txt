Amaru v10.11.20260807 / commit 493bffba0cc4db2291643cdd6698197c374958b3

crates/amaru-ledger/src/state.rs:691-703
    let tx_size = to_cbor(transaction).len() as u64;
    rules::block::validate_transaction(..., transaction, tx_size)

crates/amaru-ledger/src/rules/transaction/phase_one/mod.rs:382-385
    // Fixtures encode a standalone conway transaction (a 4-element array including the is_valid byte)
    // but the ledger expects a transaction to be the 3-element array (without the is_valid byte), so we subtract one byte.
    let tx_size = (fixture.transaction.len() - 1) as u64;

crates/amaru-ledger/tests/evaluate_ledger_states.rs:331-337
    // The transaction size is calculated from an encoded 3-element list, not including the is_valid byte.
    // The is_valid boolean is exactly one byte.
    let tx_size = (tx_bytes.len() - 1) as u64;

Stable sources:
https://github.com/pragma-org/amaru/blob/493bffba0cc4db2291643cdd6698197c374958b3/crates/amaru-ledger/src/state.rs#L691-L703
https://github.com/pragma-org/amaru/blob/493bffba0cc4db2291643cdd6698197c374958b3/crates/amaru-ledger/src/rules/transaction/phase_one/mod.rs#L382-L385
https://github.com/pragma-org/amaru/blob/493bffba0cc4db2291643cdd6698197c374958b3/crates/amaru-ledger/tests/evaluate_ledger_states.rs#L331-L337
https://github.com/pragma-org/amaru/pull/788
